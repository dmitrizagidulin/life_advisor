<%= form_for(@new_item, :html => { :class => "form-inline inline"}) do |f| %>
    <%= f.text_field :name, :class=> "project", :tabindex => 1 %>
    <%= f.submit 'Add', :class=> "project btn" %>
<% end %>