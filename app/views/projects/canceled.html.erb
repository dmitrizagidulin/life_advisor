<h1>Canceled Projects</h1>

<table class="table table-striped table-hover">
<tbody>
<% @canceled_projects.each do |project| %>
  <%= render(partial: 'project', object: project, locals: { project_status: :canceled }) %>
<% end %>
</tbody>
</table>

