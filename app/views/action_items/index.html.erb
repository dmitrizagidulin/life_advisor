<%= render partial: 'category_list', locals: { mywn_category: :critical, items: @critical_items } %>

<%= render partial: 'category_list', locals: { mywn_category: :opportunity, items: @opportunity_items } %>

<%= render partial: 'category_list', locals: { mywn_category: :horizon, items: @horizon_items } %>

<%= render partial: 'category_list', locals: { mywn_category: :someday, items: @someday_items } %>


<br />
<%= link_to 'New Action item', new_action_item_path, :class => 'btn' %>

