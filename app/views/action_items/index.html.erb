<%= render partial: 'category_list', locals: { mywn_category: :critical, items: @critical_items, tab_index: 1 } %>

<%= render partial: 'category_list', locals: { mywn_category: :tomorrow, items: @tomorrow_items, tab_index: 2 } %>

<%= render partial: 'category_list', locals: { mywn_category: :opportunity, items: @opportunity_items, tab_index: 3 } %>

<%= render partial: 'category_list', locals: { mywn_category: :horizon, items: @horizon_items, tab_index: 4 } %>

<%= render partial: 'category_list', locals: { mywn_category: :someday, items: @someday_items, tab_index: 5 } %>


<br />
<%= link_to 'New Action item', new_action_item_path, :class => 'btn' %>

