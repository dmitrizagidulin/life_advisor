<%= render partial: 'new', locals: { mywn_category: :critical } %>
<table class="table table-striped table-hover">
  <tbody>
<% @critical_items.each do |action_item| %>
	<%= render(partial: 'action_item', object: action_item) %>
<% end %>
  </tbody>
</table>
<br />

<%= render partial: 'new', locals: { mywn_category: :opportunity } %>

<table class="table table-striped">
  <tbody>
<% @opportunity_items.each do |action_item| %>
  <tr>
    <%= render(partial: 'action_item', object: action_item) %>
<% end %>
  </tbody>
</table>
<br />

<%= render partial: 'new', locals: { mywn_category: :horizon } %>

<table class="table table-striped">
  <tbody>
<% @horizon_items.each do |action_item| %>
  <%= render(partial: 'action_item', object: action_item) %>
<% end %>
  </tbody>
</table>
<br />

<%= render partial: 'new', locals: { mywn_category: :someday } %>

<table class="table table-striped">
  <tbody>
<% @someday_items.each do |action_item| %>
  <%= render(partial: 'action_item', object: action_item) %>
<% end %>
  </tbody>
</table>
<br />
<br />
<%= link_to 'New Action item', new_action_item_path, :class => 'btn' %>

